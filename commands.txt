sudo apt-get update
sudo apt-get upgrade -y
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt-get install -y nodejs
sudo npm init -y
sudo npm install
node -v
npm -v

# sudo reboot  <-- Commented out to prevent automatic restart
